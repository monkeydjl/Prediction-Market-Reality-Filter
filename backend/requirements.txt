# Runtime dependencies for backend/app/ and backend/scripts/
# Pins must install on Python 3.11 — that is what .github/workflows/ci.yml uses.
# Verified 2026-08-05 on Windows / Python 3.11.9 (3614 passed, 11 skipped).
# Generated 2026-07-02 from pip freeze + import audit

# === Web framework / server ===
fastapi==0.136.3
starlette==1.3.1
uvicorn==0.48.0
pydantic==2.13.4

# === Data / ML ===
# numpy/scipy are held below the latest release: 2.5.0 / 1.18.0 declare
# Requires-Python >=3.12, but CI (.github/workflows/ci.yml) pins 3.11.
numpy==2.4.6
scipy==1.16.3
lightgbm==4.6.0

# === Scraping / news ===
httpx==0.28.1
feedparser==6.0.12
trafilatura==1.12.2
beautifulsoup4==4.14.3
gnews==0.4.3

# === Utils / infra ===
SQLAlchemy==2.0.51
APScheduler==3.11.2
python-dotenv==1.2.2
openai==1.109.1
sentry-sdk==2.63.0
prometheus_client==0.25.0
pyzipper==0.4.0

# === Phase 9 — Accuracy Sprint ===
optuna==4.0.0
