import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
  {
    rules: {
      // The codebase marks deliberately-unused bindings with a leading
      // underscore (mock signatures that must match a real API, destructured
      // params kept for documentation). Honour that convention.
      "@typescript-eslint/no-unused-vars": [
        "warn",
        {
          argsIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          caughtErrorsIgnorePattern: "^_",
        },
      ],
    },
  },
  {
    // Generated by `python -m scripts.generate_types`; its `/* eslint-disable */`
    // banner comes from pydantic2ts. Editing the file by hand would be
    // overwritten and would break the type-sync-check CI job's --check run.
    files: ["src/lib/generated-types.ts"],
    linterOptions: { reportUnusedDisableDirectives: "off" },
  },
]);

export default eslintConfig;
