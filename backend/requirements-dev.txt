# Dev / test dependencies (not required to run the server)
# Install with: pip install -r requirements-dev.txt
# Pinned to versions verified on Windows / Python 3.14.5

pytest==9.1.1
mypy==2.1.0
pydantic-to-typescript>=2.0.0
